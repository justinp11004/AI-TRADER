# Scalper seat (0DTE · seconds matter) — per decision surface
Alert→order in ≤3 interactions, all keyboard-reachable; shortcut map discoverable (?).
At the decision point, visible without hover: NBBO, spread %, quote age, session/killzone, day-loss remaining.
Flatten & kill: one action, reachable in ≤1s from any screen; works during modal/managing states.
0DTE gates: spread gate (scalp 5%) enforced and SHOWN when blocking; theta/expiry honesty on the ticket; killzone-only entries flagged outside window.
Latency honesty: tape/quotes show staleness when stale — never freeze silently; per-tick flashes don't reflow layout.
Fast reject: one-key reject with reason enum; rejected setups journaled (LAW-018).
No surprise focus-steals mid-typing; Enter never submits capital without an armed state.
